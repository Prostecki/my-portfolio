---
name: OG image status
description: Whether an Open Graph image exists for social sharing previews
type: project
---

No og-image.png exists in /public as of the initial SEO audit (2026-04-16). The metadata in layout.tsx references /og-image.png (1200x630) which must be created manually before deployment.

Available images in /public that could be used as a starting point: avatar.png (square, not suitable as-is for OG).

**Why:** og:image and twitter:image are already wired to /og-image.png in layout.tsx. Without this file the social preview will show no image.
**How to apply:** Remind the user to create /public/og-image.png at 1200x630px before deploying. This is a manual action — it cannot be auto-generated by this agent.
